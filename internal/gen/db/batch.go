// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.17.2
// source: batch.go

package db

import (
	"context"
	"errors"

	"github.com/jackc/pgx/v5"
)

const deleteDevice = `-- name: DeleteDevice :batchexec
DELETE FROM "device"
WHERE id = $1
`

type DeleteDeviceBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

func (q *Queries) DeleteDevice(ctx context.Context, id []int32) *DeleteDeviceBatchResults {
	batch := &pgx.Batch{}
	for _, a := range id {
		vals := []interface{}{
			a,
		}
		batch.Queue(deleteDevice, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &DeleteDeviceBatchResults{br, len(id), false}
}

func (b *DeleteDeviceBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, errors.New("batch already closed"))
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *DeleteDeviceBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}
